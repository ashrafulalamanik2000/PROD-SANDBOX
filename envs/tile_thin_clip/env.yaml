# pixi (conda-forge): the script imports python-pdal, which needs the PDAL
# C++ libraries — pip can't provide them on Windows. This replaces the skill's
# mmworkflow Docker image (SETUP.md); the pipeline itself is CPU-only python.
kind: pixi
