# pixi (conda-forge) env: geopandas + pdal need GDAL/PDAL system libraries that
# pip wheels don't reliably provide on Windows. Deps come from
# tools/aecon_real/src/scripts/requirements.v2.txt (the complete list, incl. pdal).
# `sdtools env lock aecon_real` writes pixi.lock; commit both files.
kind: pixi
