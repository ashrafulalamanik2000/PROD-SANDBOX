# pixi (conda-forge) env shared by the classification and pole-vec tools —
# the CPU half of the Topology-Aerial chain. geopandas/rasterio need GDAL
# system libraries pip wheels don't reliably provide on Windows; sklearn/
# networkx serve pole discovery + network topology; scipy serves the HAG
# stratification and noise-marking k-NN. GPU stages run in Docker, not here.
# `sdtools env lock topo_chain` writes pixi.lock; commit both files.
kind: pixi
