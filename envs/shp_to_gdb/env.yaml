# pixi (conda-forge): build_gdb*.py use osgeo/ogr directly and write FileGDB
# (OpenFileGDB driver, GDAL >= 3.6 writes it natively — no ESRI SDK needed).
# Shared by both tools: shp-to-gdb (generic/DFX) and shp-to-gdb-aecon.
kind: pixi
