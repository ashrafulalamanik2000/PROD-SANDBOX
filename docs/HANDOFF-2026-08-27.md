# Session handoff — 2026-08-27 (pre-reboot, SDAI-AANIK)

State snapshot of the sdtools dispatcher deployment + pipeline work, written
just before a machine restart. Continue from "NEXT STEPS".

## Where things stand

### Dispatcher API deployment (in flight, with Codex)
- Goal: sdtools dispatcher API (Postgres + FastAPI, docker-compose) hosted on
  THIS laptop first (validation), later promoted to an always-on box.
- Codex prepared: `C:\sdtools\.env` (gitignored), `restart: unless-stopped`
  on both services, Postgres enum fix for the `submit` scope, uvicorn single
  worker (avoids duplicate sweepers), `docs\RUNBOOK-API.md`, fleet URL set to
  `http://sdai-aanik.tail094d8c.ts.net:8080` (MagicDNS is enabled).
- Claude filled BOTH secrets in `.env`: POSTGRES_PASSWORD (crypto-random,
  never displayed) and ANTHROPIC_API_KEY (user should rotate it eventually —
  it was pasted in chat).
- Docker Desktop was wedged (engine pipe missing, WSL distro Stopped);
  restarted at ~10:13. Machine reboot supersedes this — after reboot just
  start Docker Desktop.
- NOT yet done: `docker compose up -d`, minting the three scoped keys
  (submit / agent / ingest), soak-job round-trip test, kill/requeue lease
  test. Codex has these queued — tell it "secrets are filled, bring up the
  stack" after reboot.

### Console (CLI 0.4.1)
- Fleet config layer added & verified: env > ~\.sdtools\config.toml (machine;
  api KEY lives here) > C:\sdtools\config.toml (fleet; ships api.url).
  TOML reads BOM-tolerant. REMEMBER: console is a uv snapshot — code edits
  need `install.ps1` re-run.
- Tools: mobile-data-preprocessing (0.2.0, purpose-named stages
  organize/metadata/index/pano/colorize) → classification (0.1.0) →
  pole-vec (0.1.0), shared pixi env `topo_chain` (built). Four tools archived
  in C:\sdtools\tools_archived. docs\WORKFLOW.md documents everything.

### Cambridge_P3_60 (C:\Users\AshrafulAnik\Downloads\Cambridge_P3_60)
- Preprocessing COMPLETE (2026-08-26): organize/metadata/pano (2603 panos)/
  colorize → Lidar\Clipped 4 LAS, 215M pts, EPSG 26917 verified metric.
- classification inspect done: all clouds 100% unclassified → need Stage 1.
- BLOCKED at GPU gate: PointCONV stage1 + pole-vec polevec need a GPU box
  (mmworkflow image corrupts cuDNN on this Blackwell RTX 5070).

## UPDATE 2026-08-27 ~11:30 (post-reboot) — steps 1–3 DONE

- Step 1 (Codex): stack up post-reboot, /v1/health 200, keys minted
  (operator-laptop/submit, worker-agent/ingest, telemetry-ingest/ingest;
  plaintext shown once in Codex session only). Soak round-trip ok (1 attempt)
  and kill/requeue ok (LeaseExpired -> rescuer, 2 attempts) — verified in DB.
  Production timings restored (LEASE_TTL_S=120, SWEEP_INTERVAL_S=30).
- Step 2 (Claude): submit key in ~\.sdtools\config.toml [api]; `sdtools
  doctor` -> api health 200 via fleet MagicDNS url; spool backlog (15)
  flushed to 0 with `sdtools flush`; `sdtools jobs`/`agents` work.
- Step 3 (Claude): make_installer.ps1 now also stages the fleet config.toml
  (url only, no key — verified); install.ps1 machine-config stub comment
  updated (fleet ships url, machine adds key only). Built and published
  sdtools-setup-2026-08-27.exe (2.2 MB) + sdtools-2026-08-27.zip (0.6 MB) to
  \\SDAI-FS1\...\sdtools_dist. Size drop vs 231 MB build is intentional:
  envs ship as lockfiles, models external via SDAI_MODELS_DIR.
- Resume at step 4 (ML04 worker setup).

## UPDATE 2026-08-27 ~11:45 — tailnet migrated to spatialdata.ai (work)

- ML04 had joined a DIFFERENT tailnet (work, aanik@spatialdata.ai) than the
  laptop (personal nexgeo). Decision: standardize on the work tailnet.
- Laptop re-logged into the work tailnet; new domain is tail46aaf0.ts.net.
  Fleet URL is now http://sdai-aanik.tail46aaf0.ts.net:8080 (config.toml and
  .env CORS_ORIGINS updated; api container recreated; doctor -> 200).
- sdai-ml04 (100.121.70.127) visible and pinging from the laptop (DERP).
- Installer rebuilt + republished with the corrected fleet URL (the earlier
  2026-08-27 build on SDAI-FS1 carried the dead tail094d8c URL — replaced).
- TODO in Tailscale admin console: disable key expiry for sdai-aanik (still
  expires 2027-02-23) and confirm the same for sdai-ml04.

## UPDATE 2026-08-27 ~13:20 — ML04 worker live; telemetry pipeline fixed

- Step 4 DONE: ML04 agent registered (queues=gpu labels=gpu) with a fresh
  `ml04-worker` ingest key (minted via direct DB insert; plaintext delivered
  over Taildrop, deleted after use). Soak smoke test on gpu queue: ok.
- Step 5 in progress on D:\Cambridge_P3_60 (ML04): mobile-data-preprocessing
  (all stages) finished ok. classification (stage1,refine,verify,mark_noise)
  FAILED rc=1 after 2 attempts — diagnosis pending ML04's spool flush.
- GOTCHA: `sdtools submit -P` keys are manifest underscore names
  (data_root=...), not hyphenated CLI flags.

## UPDATE 2026-08-27 ~17:00 — STEP 5 DONE: Cambridge classified on ML04

- classification run e6c553c9 finished ok in 63.7 min: 215M pts, 0 never-
  classified; ground 67%/hi-veg 21%/wire+tower+pole present. Output in
  D:\Cambridge_P3_60\Lidar\Classified (ML04).
- Fixes shipped along the way (all in the republished 2026-08-27 payload):
  * classification.py find_bash() — bare "bash" resolves to the System32 WSL
    relay on boxes without a distro; prefer SDAI_BASH, then Git Bash.
  * classification.py classify_env() + topo_chain now ships pdal/laszip —
    classify.sh tiles oversize clouds (>40M pts) with pdal ($PDAL_EXE).
  * update.ps1: robocopy merge (Copy-Item -Recurse nests, silently skipping
    updates) AND marker-based root detection (the zip's top-level sdtools\ is
    the Python package, not a wrapper — old logic sprayed package files into
    the install root and updated nothing else).
- Operational: classify.sh GPU-wait gate trips on ANY --gpus all container;
  a stale topology-aerial-5000 (chain-full drop-in UI, disposable) held it
  ~35 min until docker rm -f. Consider exposing --no-gpu-wait as a tool param
  if leftovers recur.
- Remaining: pole-vec on the classified tile (GPU, ML04), step 6 API
  promotion to an always-on host.

## UPDATE 2026-08-28 — pole-vec CPU chain validated locally; cancel/retry shipped

- pole-vec debugging: crop pipeline scans input RECURSIVELY (Greg_Sandbox
  pipeline.py) -> wrapper now stages a flat pattern-matched input view
  (_work\00_input_las, hardlinks). Old linker counted already-existing
  network_in links as "0 linked" and bailed -> fixed + diagnostic message.
  Crop stage disk appetite is real: ~37 GB at half_size=50 (27.7 GB crops +
  8.8 GB tiles) for 138 poles / 28 clean clouds; budget >=50 GB free.
- LOCAL validation (C:\WORKING\Cambridge_P3_60_Classified, half_size=35):
  inspect/discover/crop/network all OK in 7.4 min — 138 poles, 179 spans,
  Network published. Clean crops: 85% ground + wire/pole classes (fixes the
  GPU stage's ground_info=None crash from unclassified-variant crops).
- NEW: cooperative cancel + retry (tested end-to-end on a local agent):
  `sdtools cancel <id>` — queued settles now; running sets cancel_requested,
  agent kills the tool tree at next lease renewal (~40 s), job settles
  cancelled (never auto-retries). `sdtools retry <id>` — requeues a failed/
  cancelled standalone job, same id, fresh attempts. Lease-loss now also
  stops the local tool (no duplicate work vs rescuer). submit keys can drive
  agent endpoints (local/dev workers). jobs table gained cancel_requested
  (ALTER applied on live DB). See RUNBOOK "Cancelling and retrying jobs".
- ML04 still needs: update.ps1 from the NAS (staging fix + new agent), then
  bounce the agent task, then rerun pole-vec (job cba67cd9 was still running
  the OLD wrapper on polluted input — cancel it once the new CLI lands).

## UPDATE 2026-08-28 ~00:40 — POLE-VEC DONE. Full pipeline complete on ML04.

- cba67cd9 attempt 2 ran the FIXED wrapper on ML04 (update + agent bounce
  had landed): staged 28 finals only, 138/138 crops clean, network built,
  and the GPU PoleVec stage completed for the first time — 85 catenary
  models, combined DXFs, 195 files -> D:\Cambridge_P3_60\Vectors\Poles\
  PoleVec (+ topology). 36.1 min, run 80c90cde.
- Cambridge_P3_60 has now been through the ENTIRE chain via the dispatcher:
  preprocessing -> classification (215M pts) -> pole network -> pole-vec
  vectorization, submitted from the laptop, executed on ML04, fully
  telemetered. Handoff steps 4 + 5: COMPLETE.
- Remaining: step 6 (promote API to an always-on host), Tailscale key-expiry
  disable on both machines, rotate the once-pasted Anthropic key in .env.
- Fixed run-telemetry pipeline (was: every write 500/403, all tables empty):
  api/db.py run-id columns -> native Uuid, Run.tags -> text[]; api/main.py
  telemetry endpoints now accept submit scope; sdtools flush reports 4xx
  drops instead of counting them as "uploaded". API image rebuilt, console
  reinstalled, installer rebuilt + republished (~13:18). Laptop-side runs
  verified landing in runs/run_events. ML04's spooled telemetry (500-failed,
  still on disk there) will replay on its next flush/run.

## NEXT STEPS (in order)
1. After reboot: start Docker Desktop; have Codex run `docker compose up -d`
   in C:\sdtools, mint 3 scoped keys (print once), run soak round-trip +
   kill/requeue tests per docs\RUNBOOK-API.md.
2. Put the submit key in this laptop's `~\.sdtools\config.toml` [api] key;
   verify `sdtools doctor` → api health 200.
3. Rebuild the installer (stage to C:\sdt_stage, ISCC, per memory recipe) so
   the payload carries the new tools + fleet config; publish to SDAI-FS1.
4. ML04: install Tailscale (join tailnet, tag as worker, disable key expiry),
   run installer, set SDAI_CLASSIFY_SRC / SDAI_MODELS_DIR / SDAI_CHAIN_SRC,
   drop agent key in ~\.sdtools\config.toml, start
   `sdtools agent --queue gpu --labels gpu` (as a scheduled task).
5. Copy Cambridge Lidar\Clipped (7.2 GB) to ML04, then from the laptop:
   `sdtools submit classification --queue gpu --require-labels gpu
    --data-root <tile-on-ml04> --epsg 26917 --stages stage1,refine,verify --mark-noise`
6. Later: promote API off the laptop to an always-on host (pg_dump/restore,
   flip fleet config url, rebuild installer). Tailscale Starter for
   individual work logins; 11 machines are free (priced per user).
